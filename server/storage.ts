// This storage layer has been retired in favor of proxying to the Django backend.
// Keeping an empty stub to avoid confusion and accidental imports.

export const storage = {
  // No-op methods retained solely for type compatibility if imported somewhere
};
